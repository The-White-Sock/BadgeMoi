<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Thème XML minimal : tout le design (couleurs jour/nuit, typographie) est
      piloté par Compose via BadgeMoiTheme (ui/theme). Ce thème ne sert qu'à
      fixer la fenêtre avant le premier rendu Compose.
    -->
    <style name="Theme.BadgeMoi" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
        <item name="android:navigationBarColor">@color/window_background</item>
    </style>
</resources>
