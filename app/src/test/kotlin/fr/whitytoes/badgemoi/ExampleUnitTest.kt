package fr.whitytoes.badgemoi

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Test de fumée : confirme que la chaîne de build/exécution des tests unitaires
 * fonctionne. À remplacer par les vrais tests du modèle de trajet au lot 1.
 */
class ExampleUnitTest {
    @Test
    fun `addition fonctionne`() {
        assertEquals(4, 2 + 2)
    }
}
